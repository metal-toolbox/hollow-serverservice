// Package gormdb provides all connectivity with the database. This is an internal package as it is not meant to be used
// outside of governor directly. All access to the data store from external of hollow will happen via the api package,
// which provides versioned resources.
package gormdb // import go.metalkube.net/hollow/internal/gormdb
