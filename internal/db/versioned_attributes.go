// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package db

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// VersionedAttribute is an object representing the database table.
type VersionedAttribute struct {
	ID                string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	ServerID          null.String `boil:"server_id" json:"server_id,omitempty" toml:"server_id" yaml:"server_id,omitempty"`
	Namespace         string      `boil:"namespace" json:"namespace" toml:"namespace" yaml:"namespace"`
	Data              types.JSON  `boil:"data" json:"data" toml:"data" yaml:"data"`
	CreatedAt         null.Time   `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`
	UpdatedAt         null.Time   `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`
	ServerComponentID null.String `boil:"server_component_id" json:"server_component_id,omitempty" toml:"server_component_id" yaml:"server_component_id,omitempty"`
	Tally             int64       `boil:"tally" json:"tally" toml:"tally" yaml:"tally"`

	R *versionedAttributeR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L versionedAttributeL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var VersionedAttributeColumns = struct {
	ID                string
	ServerID          string
	Namespace         string
	Data              string
	CreatedAt         string
	UpdatedAt         string
	ServerComponentID string
	Tally             string
}{
	ID:                "id",
	ServerID:          "server_id",
	Namespace:         "namespace",
	Data:              "data",
	CreatedAt:         "created_at",
	UpdatedAt:         "updated_at",
	ServerComponentID: "server_component_id",
	Tally:             "tally",
}

var VersionedAttributeTableColumns = struct {
	ID                string
	ServerID          string
	Namespace         string
	Data              string
	CreatedAt         string
	UpdatedAt         string
	ServerComponentID string
	Tally             string
}{
	ID:                "versioned_attributes.id",
	ServerID:          "versioned_attributes.server_id",
	Namespace:         "versioned_attributes.namespace",
	Data:              "versioned_attributes.data",
	CreatedAt:         "versioned_attributes.created_at",
	UpdatedAt:         "versioned_attributes.updated_at",
	ServerComponentID: "versioned_attributes.server_component_id",
	Tally:             "versioned_attributes.tally",
}

// Generated where

type whereHelperint64 struct{ field string }

func (w whereHelperint64) EQ(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperint64) NEQ(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperint64) LT(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperint64) LTE(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperint64) GT(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperint64) GTE(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperint64) IN(slice []int64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperint64) NIN(slice []int64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

var VersionedAttributeWhere = struct {
	ID                whereHelperstring
	ServerID          whereHelpernull_String
	Namespace         whereHelperstring
	Data              whereHelpertypes_JSON
	CreatedAt         whereHelpernull_Time
	UpdatedAt         whereHelpernull_Time
	ServerComponentID whereHelpernull_String
	Tally             whereHelperint64
}{
	ID:                whereHelperstring{field: "\"versioned_attributes\".\"id\""},
	ServerID:          whereHelpernull_String{field: "\"versioned_attributes\".\"server_id\""},
	Namespace:         whereHelperstring{field: "\"versioned_attributes\".\"namespace\""},
	Data:              whereHelpertypes_JSON{field: "\"versioned_attributes\".\"data\""},
	CreatedAt:         whereHelpernull_Time{field: "\"versioned_attributes\".\"created_at\""},
	UpdatedAt:         whereHelpernull_Time{field: "\"versioned_attributes\".\"updated_at\""},
	ServerComponentID: whereHelpernull_String{field: "\"versioned_attributes\".\"server_component_id\""},
	Tally:             whereHelperint64{field: "\"versioned_attributes\".\"tally\""},
}

// VersionedAttributeRels is where relationship names are stored.
var VersionedAttributeRels = struct {
	Server          string
	ServerComponent string
}{
	Server:          "Server",
	ServerComponent: "ServerComponent",
}

// versionedAttributeR is where relationships are stored.
type versionedAttributeR struct {
	Server          *Server          `boil:"Server" json:"Server" toml:"Server" yaml:"Server"`
	ServerComponent *ServerComponent `boil:"ServerComponent" json:"ServerComponent" toml:"ServerComponent" yaml:"ServerComponent"`
}

// NewStruct creates a new relationship struct
func (*versionedAttributeR) NewStruct() *versionedAttributeR {
	return &versionedAttributeR{}
}

// versionedAttributeL is where Load methods for each relationship are stored.
type versionedAttributeL struct{}

var (
	versionedAttributeAllColumns            = []string{"id", "server_id", "namespace", "data", "created_at", "updated_at", "server_component_id", "tally"}
	versionedAttributeColumnsWithoutDefault = []string{"namespace", "data"}
	versionedAttributeColumnsWithDefault    = []string{"id", "server_id", "created_at", "updated_at", "server_component_id", "tally"}
	versionedAttributePrimaryKeyColumns     = []string{"id"}
)

type (
	// VersionedAttributeSlice is an alias for a slice of pointers to VersionedAttribute.
	// This should almost always be used instead of []VersionedAttribute.
	VersionedAttributeSlice []*VersionedAttribute
	// VersionedAttributeHook is the signature for custom VersionedAttribute hook methods
	VersionedAttributeHook func(context.Context, boil.ContextExecutor, *VersionedAttribute) error

	versionedAttributeQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	versionedAttributeType                 = reflect.TypeOf(&VersionedAttribute{})
	versionedAttributeMapping              = queries.MakeStructMapping(versionedAttributeType)
	versionedAttributePrimaryKeyMapping, _ = queries.BindMapping(versionedAttributeType, versionedAttributeMapping, versionedAttributePrimaryKeyColumns)
	versionedAttributeInsertCacheMut       sync.RWMutex
	versionedAttributeInsertCache          = make(map[string]insertCache)
	versionedAttributeUpdateCacheMut       sync.RWMutex
	versionedAttributeUpdateCache          = make(map[string]updateCache)
	versionedAttributeUpsertCacheMut       sync.RWMutex
	versionedAttributeUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var versionedAttributeBeforeInsertHooks []VersionedAttributeHook
var versionedAttributeBeforeUpdateHooks []VersionedAttributeHook
var versionedAttributeBeforeDeleteHooks []VersionedAttributeHook
var versionedAttributeBeforeUpsertHooks []VersionedAttributeHook

var versionedAttributeAfterInsertHooks []VersionedAttributeHook
var versionedAttributeAfterSelectHooks []VersionedAttributeHook
var versionedAttributeAfterUpdateHooks []VersionedAttributeHook
var versionedAttributeAfterDeleteHooks []VersionedAttributeHook
var versionedAttributeAfterUpsertHooks []VersionedAttributeHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *VersionedAttribute) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range versionedAttributeBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *VersionedAttribute) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range versionedAttributeBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *VersionedAttribute) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range versionedAttributeBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *VersionedAttribute) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range versionedAttributeBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *VersionedAttribute) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range versionedAttributeAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *VersionedAttribute) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range versionedAttributeAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *VersionedAttribute) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range versionedAttributeAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *VersionedAttribute) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range versionedAttributeAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *VersionedAttribute) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range versionedAttributeAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddVersionedAttributeHook registers your hook function for all future operations.
func AddVersionedAttributeHook(hookPoint boil.HookPoint, versionedAttributeHook VersionedAttributeHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		versionedAttributeBeforeInsertHooks = append(versionedAttributeBeforeInsertHooks, versionedAttributeHook)
	case boil.BeforeUpdateHook:
		versionedAttributeBeforeUpdateHooks = append(versionedAttributeBeforeUpdateHooks, versionedAttributeHook)
	case boil.BeforeDeleteHook:
		versionedAttributeBeforeDeleteHooks = append(versionedAttributeBeforeDeleteHooks, versionedAttributeHook)
	case boil.BeforeUpsertHook:
		versionedAttributeBeforeUpsertHooks = append(versionedAttributeBeforeUpsertHooks, versionedAttributeHook)
	case boil.AfterInsertHook:
		versionedAttributeAfterInsertHooks = append(versionedAttributeAfterInsertHooks, versionedAttributeHook)
	case boil.AfterSelectHook:
		versionedAttributeAfterSelectHooks = append(versionedAttributeAfterSelectHooks, versionedAttributeHook)
	case boil.AfterUpdateHook:
		versionedAttributeAfterUpdateHooks = append(versionedAttributeAfterUpdateHooks, versionedAttributeHook)
	case boil.AfterDeleteHook:
		versionedAttributeAfterDeleteHooks = append(versionedAttributeAfterDeleteHooks, versionedAttributeHook)
	case boil.AfterUpsertHook:
		versionedAttributeAfterUpsertHooks = append(versionedAttributeAfterUpsertHooks, versionedAttributeHook)
	}
}

// One returns a single versionedAttribute record from the query.
func (q versionedAttributeQuery) One(ctx context.Context, exec boil.ContextExecutor) (*VersionedAttribute, error) {
	o := &VersionedAttribute{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "db: failed to execute a one query for versioned_attributes")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all VersionedAttribute records from the query.
func (q versionedAttributeQuery) All(ctx context.Context, exec boil.ContextExecutor) (VersionedAttributeSlice, error) {
	var o []*VersionedAttribute

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "db: failed to assign all query results to VersionedAttribute slice")
	}

	if len(versionedAttributeAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all VersionedAttribute records in the query.
func (q versionedAttributeQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "db: failed to count versioned_attributes rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q versionedAttributeQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "db: failed to check if versioned_attributes exists")
	}

	return count > 0, nil
}

// Server pointed to by the foreign key.
func (o *VersionedAttribute) Server(mods ...qm.QueryMod) serverQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ServerID),
	}

	queryMods = append(queryMods, mods...)

	query := Servers(queryMods...)
	queries.SetFrom(query.Query, "\"servers\"")

	return query
}

// ServerComponent pointed to by the foreign key.
func (o *VersionedAttribute) ServerComponent(mods ...qm.QueryMod) serverComponentQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ServerComponentID),
	}

	queryMods = append(queryMods, mods...)

	query := ServerComponents(queryMods...)
	queries.SetFrom(query.Query, "\"server_components\"")

	return query
}

// LoadServer allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (versionedAttributeL) LoadServer(ctx context.Context, e boil.ContextExecutor, singular bool, maybeVersionedAttribute interface{}, mods queries.Applicator) error {
	var slice []*VersionedAttribute
	var object *VersionedAttribute

	if singular {
		object = maybeVersionedAttribute.(*VersionedAttribute)
	} else {
		slice = *maybeVersionedAttribute.(*[]*VersionedAttribute)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &versionedAttributeR{}
		}
		if !queries.IsNil(object.ServerID) {
			args = append(args, object.ServerID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &versionedAttributeR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.ServerID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.ServerID) {
				args = append(args, obj.ServerID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`servers`),
		qm.WhereIn(`servers.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Server")
	}

	var resultSlice []*Server
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Server")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for servers")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for servers")
	}

	if len(versionedAttributeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Server = foreign
		if foreign.R == nil {
			foreign.R = &serverR{}
		}
		foreign.R.VersionedAttributes = append(foreign.R.VersionedAttributes, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.ServerID, foreign.ID) {
				local.R.Server = foreign
				if foreign.R == nil {
					foreign.R = &serverR{}
				}
				foreign.R.VersionedAttributes = append(foreign.R.VersionedAttributes, local)
				break
			}
		}
	}

	return nil
}

// LoadServerComponent allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (versionedAttributeL) LoadServerComponent(ctx context.Context, e boil.ContextExecutor, singular bool, maybeVersionedAttribute interface{}, mods queries.Applicator) error {
	var slice []*VersionedAttribute
	var object *VersionedAttribute

	if singular {
		object = maybeVersionedAttribute.(*VersionedAttribute)
	} else {
		slice = *maybeVersionedAttribute.(*[]*VersionedAttribute)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &versionedAttributeR{}
		}
		if !queries.IsNil(object.ServerComponentID) {
			args = append(args, object.ServerComponentID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &versionedAttributeR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.ServerComponentID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.ServerComponentID) {
				args = append(args, obj.ServerComponentID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`server_components`),
		qm.WhereIn(`server_components.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load ServerComponent")
	}

	var resultSlice []*ServerComponent
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice ServerComponent")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for server_components")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for server_components")
	}

	if len(versionedAttributeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.ServerComponent = foreign
		if foreign.R == nil {
			foreign.R = &serverComponentR{}
		}
		foreign.R.VersionedAttributes = append(foreign.R.VersionedAttributes, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.ServerComponentID, foreign.ID) {
				local.R.ServerComponent = foreign
				if foreign.R == nil {
					foreign.R = &serverComponentR{}
				}
				foreign.R.VersionedAttributes = append(foreign.R.VersionedAttributes, local)
				break
			}
		}
	}

	return nil
}

// SetServer of the versionedAttribute to the related item.
// Sets o.R.Server to related.
// Adds o to related.R.VersionedAttributes.
func (o *VersionedAttribute) SetServer(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Server) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"versioned_attributes\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"server_id"}),
		strmangle.WhereClause("\"", "\"", 2, versionedAttributePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.ServerID, related.ID)
	if o.R == nil {
		o.R = &versionedAttributeR{
			Server: related,
		}
	} else {
		o.R.Server = related
	}

	if related.R == nil {
		related.R = &serverR{
			VersionedAttributes: VersionedAttributeSlice{o},
		}
	} else {
		related.R.VersionedAttributes = append(related.R.VersionedAttributes, o)
	}

	return nil
}

// RemoveServer relationship.
// Sets o.R.Server to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *VersionedAttribute) RemoveServer(ctx context.Context, exec boil.ContextExecutor, related *Server) error {
	var err error

	queries.SetScanner(&o.ServerID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("server_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Server = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.VersionedAttributes {
		if queries.Equal(o.ServerID, ri.ServerID) {
			continue
		}

		ln := len(related.R.VersionedAttributes)
		if ln > 1 && i < ln-1 {
			related.R.VersionedAttributes[i] = related.R.VersionedAttributes[ln-1]
		}
		related.R.VersionedAttributes = related.R.VersionedAttributes[:ln-1]
		break
	}
	return nil
}

// SetServerComponent of the versionedAttribute to the related item.
// Sets o.R.ServerComponent to related.
// Adds o to related.R.VersionedAttributes.
func (o *VersionedAttribute) SetServerComponent(ctx context.Context, exec boil.ContextExecutor, insert bool, related *ServerComponent) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"versioned_attributes\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"server_component_id"}),
		strmangle.WhereClause("\"", "\"", 2, versionedAttributePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.ServerComponentID, related.ID)
	if o.R == nil {
		o.R = &versionedAttributeR{
			ServerComponent: related,
		}
	} else {
		o.R.ServerComponent = related
	}

	if related.R == nil {
		related.R = &serverComponentR{
			VersionedAttributes: VersionedAttributeSlice{o},
		}
	} else {
		related.R.VersionedAttributes = append(related.R.VersionedAttributes, o)
	}

	return nil
}

// RemoveServerComponent relationship.
// Sets o.R.ServerComponent to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *VersionedAttribute) RemoveServerComponent(ctx context.Context, exec boil.ContextExecutor, related *ServerComponent) error {
	var err error

	queries.SetScanner(&o.ServerComponentID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("server_component_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.ServerComponent = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.VersionedAttributes {
		if queries.Equal(o.ServerComponentID, ri.ServerComponentID) {
			continue
		}

		ln := len(related.R.VersionedAttributes)
		if ln > 1 && i < ln-1 {
			related.R.VersionedAttributes[i] = related.R.VersionedAttributes[ln-1]
		}
		related.R.VersionedAttributes = related.R.VersionedAttributes[:ln-1]
		break
	}
	return nil
}

// VersionedAttributes retrieves all the records using an executor.
func VersionedAttributes(mods ...qm.QueryMod) versionedAttributeQuery {
	mods = append(mods, qm.From("\"versioned_attributes\""))
	return versionedAttributeQuery{NewQuery(mods...)}
}

// FindVersionedAttribute retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindVersionedAttribute(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*VersionedAttribute, error) {
	versionedAttributeObj := &VersionedAttribute{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"versioned_attributes\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, versionedAttributeObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "db: unable to select from versioned_attributes")
	}

	if err = versionedAttributeObj.doAfterSelectHooks(ctx, exec); err != nil {
		return versionedAttributeObj, err
	}

	return versionedAttributeObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *VersionedAttribute) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("db: no versioned_attributes provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		if queries.MustTime(o.UpdatedAt).IsZero() {
			queries.SetScanner(&o.UpdatedAt, currTime)
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(versionedAttributeColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	versionedAttributeInsertCacheMut.RLock()
	cache, cached := versionedAttributeInsertCache[key]
	versionedAttributeInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			versionedAttributeAllColumns,
			versionedAttributeColumnsWithDefault,
			versionedAttributeColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(versionedAttributeType, versionedAttributeMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(versionedAttributeType, versionedAttributeMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"versioned_attributes\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"versioned_attributes\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "db: unable to insert into versioned_attributes")
	}

	if !cached {
		versionedAttributeInsertCacheMut.Lock()
		versionedAttributeInsertCache[key] = cache
		versionedAttributeInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the VersionedAttribute.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *VersionedAttribute) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	versionedAttributeUpdateCacheMut.RLock()
	cache, cached := versionedAttributeUpdateCache[key]
	versionedAttributeUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			versionedAttributeAllColumns,
			versionedAttributePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("db: unable to update versioned_attributes, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"versioned_attributes\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, versionedAttributePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(versionedAttributeType, versionedAttributeMapping, append(wl, versionedAttributePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to update versioned_attributes row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "db: failed to get rows affected by update for versioned_attributes")
	}

	if !cached {
		versionedAttributeUpdateCacheMut.Lock()
		versionedAttributeUpdateCache[key] = cache
		versionedAttributeUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q versionedAttributeQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to update all for versioned_attributes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to retrieve rows affected for versioned_attributes")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o VersionedAttributeSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("db: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), versionedAttributePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"versioned_attributes\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, versionedAttributePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to update all in versionedAttribute slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to retrieve rows affected all in update all versionedAttribute")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *VersionedAttribute) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("db: no versioned_attributes provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(versionedAttributeColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	versionedAttributeUpsertCacheMut.RLock()
	cache, cached := versionedAttributeUpsertCache[key]
	versionedAttributeUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			versionedAttributeAllColumns,
			versionedAttributeColumnsWithDefault,
			versionedAttributeColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			versionedAttributeAllColumns,
			versionedAttributePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("db: unable to upsert versioned_attributes, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(versionedAttributePrimaryKeyColumns))
			copy(conflict, versionedAttributePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryCockroachDB(dialect, "\"versioned_attributes\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(versionedAttributeType, versionedAttributeMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(versionedAttributeType, versionedAttributeMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		_, _ = fmt.Fprintln(boil.DebugWriter, cache.query)
		_, _ = fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // CockcorachDB doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "db: unable to upsert versioned_attributes")
	}

	if !cached {
		versionedAttributeUpsertCacheMut.Lock()
		versionedAttributeUpsertCache[key] = cache
		versionedAttributeUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single VersionedAttribute record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *VersionedAttribute) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("db: no VersionedAttribute provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), versionedAttributePrimaryKeyMapping)
	sql := "DELETE FROM \"versioned_attributes\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to delete from versioned_attributes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "db: failed to get rows affected by delete for versioned_attributes")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q versionedAttributeQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("db: no versionedAttributeQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to delete all from versioned_attributes")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "db: failed to get rows affected by deleteall for versioned_attributes")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o VersionedAttributeSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(versionedAttributeBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), versionedAttributePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"versioned_attributes\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, versionedAttributePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to delete all from versionedAttribute slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "db: failed to get rows affected by deleteall for versioned_attributes")
	}

	if len(versionedAttributeAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *VersionedAttribute) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindVersionedAttribute(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *VersionedAttributeSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := VersionedAttributeSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), versionedAttributePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"versioned_attributes\".* FROM \"versioned_attributes\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, versionedAttributePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "db: unable to reload all in VersionedAttributeSlice")
	}

	*o = slice

	return nil
}

// VersionedAttributeExists checks if the VersionedAttribute row exists.
func VersionedAttributeExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"versioned_attributes\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "db: unable to check if versioned_attributes exists")
	}

	return exists, nil
}
